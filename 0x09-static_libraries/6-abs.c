#include "main.h"
/**
 *_abs - function to print absolute of integer
 *
 *@n: integer
 *
 *
 *Return: to success
 */
int _abs(int n)
{
	if (n <= 0)
	{
		n = n * (-1);
		return (n);
	}
	else
	{
		return (n);
	}
	return (0);
}
